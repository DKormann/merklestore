{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && BUILD_TARGET=gh-pages vite build --force",
    "build:gh-pages": "BUILD_TARGET=gh-pages vite build --force",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "postinstall": "vite --version"
  },
  "dependencies": {
    "@clockworklabs/spacetimedb-sdk": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  }
}